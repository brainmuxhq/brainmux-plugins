{
  "name": "brainmux",
  "version": "0.1.0",
  "private": true,
  "description": "brainmux — Claude Code plugin marketplace (LLM tooling).",
  "license": "MIT",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "guard": "node scripts/version-sync-guard.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
