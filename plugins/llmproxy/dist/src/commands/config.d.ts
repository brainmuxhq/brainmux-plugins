import { type Paths } from "../core/paths.js";
export declare function addBrain(paths: Paths, name: string, port: number, model: string, providerKey: string): void;
export declare function runConfig(sub: string, rest: string[], env?: NodeJS.ProcessEnv): number;
