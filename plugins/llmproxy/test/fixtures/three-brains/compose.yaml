# GENERATED by bmux from brains.yaml — do not hand-edit.
# Edit brains.yaml then run `bmux up` (regenerates) or `bmux config ...`.

x-litellm-base: &litellm-base
  image: ghcr.io/brainmuxhq/brainmux-litellm@sha256:e53c8f4f012fe1286fcfd78b6a108cdf0865af21f735dd10cff47df93bf9f23f
  restart: unless-stopped
  depends_on:
    postgres:
      condition: service_healthy
  networks: [brainmux]
  command: ["--config", "/app/config.yaml"]
  logging:
    driver: json-file
    options: { max-size: "10m", max-file: "3" }
  healthcheck:
    test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://localhost:4000/health/liveliness')\""]
    interval: 15s
    timeout: 5s
    retries: 5
    start_period: 30s

services:
  postgres:
    image: postgres:16-alpine
    container_name: brainmux-postgres
    restart: unless-stopped
    networks: [brainmux]
    logging:
      driver: json-file
      options: { max-size: "10m", max-file: "3" }
    environment:
      POSTGRES_USER: litellm
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: litellm
    volumes:
      - ../data/postgres:/var/lib/postgresql/data
      - ./init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U litellm"]
      interval: 5s
      timeout: 5s
      retries: 10
  chat:
    <<: *litellm-base
    container_name: brainmux-chat
    ports:
      - "127.0.0.1:4567:4000"
    environment:
      LITELLM_SALT_KEY: ${LITELLM_SALT_KEY}
      STORE_MODEL_IN_DB: "True"
      DATABASE_URL: postgresql://litellm:${POSTGRES_PASSWORD}@postgres:5432/litellm_chat
      LITELLM_MASTER_KEY: ${CHAT_MASTER_KEY}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
    volumes:
      - ./chat.yaml:/app/config.yaml:ro
  deep:
    <<: *litellm-base
    container_name: brainmux-deep
    ports:
      - "127.0.0.1:4568:4000"
    environment:
      LITELLM_SALT_KEY: ${LITELLM_SALT_KEY}
      STORE_MODEL_IN_DB: "True"
      DATABASE_URL: postgresql://litellm:${POSTGRES_PASSWORD}@postgres:5432/litellm_deep
      LITELLM_MASTER_KEY: ${DEEP_MASTER_KEY}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
    volumes:
      - ./deep.yaml:/app/config.yaml:ro
  coder:
    <<: *litellm-base
    container_name: brainmux-coder
    ports:
      - "127.0.0.1:4569:4000"
    environment:
      LITELLM_SALT_KEY: ${LITELLM_SALT_KEY}
      STORE_MODEL_IN_DB: "True"
      DATABASE_URL: postgresql://litellm:${POSTGRES_PASSWORD}@postgres:5432/litellm_coder
      LITELLM_MASTER_KEY: ${CODER_MASTER_KEY}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY}
    volumes:
      - ./coder.yaml:/app/config.yaml:ro
networks:
  brainmux:
    name: brainmux
