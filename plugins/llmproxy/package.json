{
  "name": "@brainmux/llmproxy",
  "version": "0.0.0",
  "description": "Run Claude Code on cheap/alternate LLM brains and delegate grunt work to save your Opus quota.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "bmux": "bin/bmux"
  },
  "files": ["dist", "bin", "templates", "skills", "commands", ".claude-plugin", ".env.example", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "esbuild src/cli.ts --bundle --platform=node --format=esm --outfile=dist/bmux.js",
    "prepare": "npm run build && npm run bundle",
    "dist-check": "npm run bundle && git diff --exit-code -- dist/bmux.js",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\""
  },
  "dependencies": {
    "yaml": "^2.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.24.0"
  },
  "engines": { "node": ">=18" }
}
