# secrets — NEVER commit (public repo)
.env
.env.local
.env.*.local
*.pem
*.key

# state / data
data/
.brainmux/

# node
node_modules/
dist/
# ...except the llmproxy plugin's compiled CLI — it ships with the plugin so a
# marketplace-from-git install runs with no build step (see Plan 2 spec §2).
# Only the runtime CLI (dist/src) ships; compiled tests (dist/test) stay ignored.
!plugins/llmproxy/dist/
!plugins/llmproxy/dist/**
plugins/llmproxy/dist/test/
build/
*.tsbuildinfo
.npm/
npm-debug.log*
pnpm-debug.log*
yarn-error.log*

# os / editor
.DS_Store
*.swp
.idea/
.vscode/
